package picker

// Sync vendored shaders from the submodule before releasing.
// Run: go generate ./internal/picker/...
//
//go:generate sh -c "rm -f ghostty-shaders-dist/*.glsl && cp ghostty-shaders/*.glsl ghostty-shaders-dist/"
